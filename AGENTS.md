<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# BloomPilot Working Context

Before doing substantial work, read:

1. `PROJECT_CONTEXT.md`
2. `docs/current-state.md`
3. `docs/next-phase.md`

Rules for future sessions:

- Treat those files as the repo's stable working memory.
- Do not restate stable project context back to the user unless asked.
- Default to implementation-first behavior.
- Keep explanations short unless the user explicitly asks for depth.
- Prefer updating the context docs when the project phase, architecture, routes, or deployment status changes.
- When starting new work, use the docs above instead of re-deriving product, stack, or phase details.
