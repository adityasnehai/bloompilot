import { GardenStudio } from "./studio";

export const metadata = {
  title: "Placement Studio · BloomPilot",
  description: "Plan plant placement around light and watering, then save it to your garden.",
};

export default function GardenStudioPage() {
  return <GardenStudio />;
}
